import { Color } from './types';

export const LOCAL_STORAGE_PREFIX = 'crs';

export const playerColors: Record<number, string> = {
  [Color.RED]: '#f00',
  [Color.BLUE]: '#00f',
  [Color.GREEN]: '#0f0',
  [Color.YELLOW]: '#ff0',
  [Color.BLACK]: '#000',
};
